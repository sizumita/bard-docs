# サーバー辞書用コマンド

## `/dict 表示`

読み上げの際に辞書を使って文字列を変換します（`Bard` -> `バード読み上げBot` など）。

その一覧を表示します。

## `/dict 追加 [変換する文字] [変換先文字]`

追加すると、 `変換する文字` を `変換先文字` と読み上げるようになります。

例) `/dict 追加 Bard バード読み上げBot`

## `/dict 削除 [変換する文字]`

サーバー辞書から単語を削除します。

例) `/dict 削除 Bard`